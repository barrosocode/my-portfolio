// Imports
import Grid from "@mui/material/Grid2";

// Component

const CenterContainer = () => {
    return <Grid container spacing={5} alignItems="center" justifyContent="center" style={{minWidth: "100%"}} textAlign={"center"}></Grid>;
};

// Export
export default CenterContainer;
